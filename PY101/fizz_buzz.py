'''Write a program that asks the user to enter an integer greater 
than 0, then asks whether the user wants to determine 
the sum or the product of all numbers between 1 and the 
entered integer, inclusive.

 5
Enter "s" to compute the sum, or "p" to compute the product. s

The sum of the integers between 1 and 5 is 15.'''

user_input = input('Please enter an integer greater than 0: ')
user_input = int(user_input)
print(input("Type 's' to determine the sum of " ))